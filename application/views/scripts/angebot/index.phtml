<?php
$offers = $this->offers;
if (empty ( $offers )) {
	echo 'Sie haben noch keine Angebote angefordert!';
}
if (! empty ( $offers )) {
	print <<<tablehead
		<h2>Meine Angebote</h2>
<table border="1">
	<tr>
		<th>Angebotsnummer</th>
		<th>Erstelldatum</th>
		<th>&#160;</th>
	</tr>
tablehead;
	foreach ( $offers as $offer ) {
		// $art = $offer->getAngebot ();
		echo '<tr><td>' . $offer->getId () . '</td>';
		// echo '<td>' . $offer->getErstelldatum () . '</td>';
		echo '<td>erstelldatum</td>';
		echo '<td><a href="/Angebot/anzeigen/id/' . $offer->getId () . '">Anzeigen</a></td></tr>';
	}
	echo '</table>';
}
var_dump ( $offers );
?>