<?php
$offers = $this->offers;
if (empty ( $offers )) {
	echo 'Sie haben noch keine Angebote angefordert!';
}
if (! empty ( $offers )) {
	print <<<tablehead
		<fieldset id="inhalte"><legend><h2>Meine Angebote</h2></legend>

tablehead;
	$rowCssIdx = 0; // odd or even row
	foreach ( $offers as $offer ) {
		++ $rowCssIdx;
		$rowCssClass = $rowCssIdx % 2 ? 'even' : 'odd';
		$art = $offer->getAngebot ();
		$dat = $art[0]->getErstelldatum();
		$amount = count($offer->getAngebot());
		echo '<p class="'.$rowCssClass.'">';
		echo 'Angebotsnummer: '.$offer->getID().'<br>';
		echo 'Erstelldatum: '.$dat.'<br>';
		echo 'Anzahl der Artikel: '.$amount;
		echo '&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;';
		echo '<a href="/Angebot/anzeigen/id/' . $offer->getId () . '">Anzeigen</a> | ';
		echo '<a href="/Angebot/remove/id/' . $offer->getId () . '">Verwerfern</a>';
		echo '<br></p><hr>';
		/*echo '<tr class="'.$rowCssClass.'">';
		echo '<td>' . $offer->getId () . '</td>';
		$art = $offer->getAngebot ();
		$dat = $art[0]->getErstelldatum();
		echo '<td>'.$dat.'</td>';
		echo '<td><a href="/Angebot/anzeigen/id/' . $offer->getId () . '">Anzeigen</a></td>';
		echo '<td></td></tr>';*/
	}
	echo '</fieldset>';
}

?>